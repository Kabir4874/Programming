#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int stairs(int n)
{
    if (n < 0)
    {
        return 0;
    }
    if (n == 0)
    {
        return 1;
    }
}
int main()
{
    int n;
    cin >> n;
    int ans = stairs(n);
    return 0;
}